<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OwnMouse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_blue</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Override button style only -->
        <item name="android:buttonStyle">@style/FlatButton</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="FlatButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/cornered_square</item>
        <item name="android:stateListAnimator">@null</item> <!-- removes elevation -->
    </style>

    <style name="Theme.OwnMouse" parent="Base.Theme.OwnMouse" />
</resources>